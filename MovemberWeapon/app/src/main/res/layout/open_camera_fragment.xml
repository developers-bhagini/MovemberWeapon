<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_back_ground">

    <ImageView
        android:layout_width="344dp"
        android:layout_height="115dp"
        app:srcCompat="@drawable/menu_tab"
        android:id="@+id/action_bar_image_id"
        tools:layout_editor_absoluteY="8dp"
        tools:layout_editor_absoluteX="8dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:id="@+id/action_bar_button_id"
        app:layout_constraintBottom_toBottomOf="@+id/action_bar_image_id"
        app:layout_constraintTop_toTopOf="@+id/action_bar_image_id"
        android:layout_marginStart="24dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="24dp" />

    <ImageView
        android:layout_width="251dp"
        android:layout_height="66dp"
        app:srcCompat="@drawable/open_camera_button"
        android:id="@+id/open_camera_id"
        app:layout_constraintTop_toBottomOf="@+id/action_bar_image_id"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="16dp" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="277dp"
        app:srcCompat="@drawable/open_camera_image"
        android:id="@+id/open_camera_image_id"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/open_camera_id"
        android:scaleType="fitStart"
        app:layout_constraintBottom_toBottomOf="parent"
        android:adjustViewBounds="false"
        android:cropToPadding="false"
        android:layout_marginBottom="8dp" />

</android.support.constraint.ConstraintLayout>
